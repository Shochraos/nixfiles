---
condition: "**/README.md"
interruptMode: never
---
Editing a README. Baseline from `skill://create-readme`: keep hand-written
sections that are still accurate, rewrite only what the change made stale.
Follow the edit with an audit via `skill://avoid-ai-writing` so the result does
not read as machine-generated prose.
