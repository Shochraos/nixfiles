---
condition: "*.py"
interruptMode: never
---
Editing a Python file — the Python pack applies. Before writing more Python,
read the core five via `skill://`: python-code-style, python-anti-patterns,
python-testing-patterns, python-error-handling, python-type-safety.

Pull the matching domain skill when the subtask touches it: python-design-patterns
(service/component design, refactor evaluations), python-project-structure and
python-packaging and uv-package-manager (layout, packaging, dependencies),
python-configuration (env vars, typed settings, secrets), python-observability
(logging, metrics, tracing), python-performance-optimization (profiling),
python-resilience (retries, timeouts), python-resource-management (context
managers, cleanup, streaming), python-background-jobs (queues, workers),
async-python-patterns (asyncio, concurrency).
