---
condition:
  - "*.sh"
  - "*.bash"
interruptMode: never
---
Editing a shell script. Read `skill://bash-defensive-patterns` and
`skill://shellcheck-configuration` before writing more shell, and
`skill://bats-testing-patterns` when adding tests for one. For scripts that
drive pactl or PipeWire, also read `skill://pactl-bash-daemon`.
